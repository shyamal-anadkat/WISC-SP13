//ld instruction test 1
//this assumes that the andni, addi, and st instructions work

//write zeros to all bits, then read. check r3 for the result.

andni r1, r1, 0x0 //fill with ones
addi r0, r1, 30 //address
st r1, r0, 0x0
ld r3,r0,0x0
halt
