//ld instruction test 0
//this assumes that the lbi, slbi, and st instructions work

//write ones to all bits, then read. check r3 for the result.

lbi r1, 0xFF //fill r1 with ones
lbi r0, 0x50 //address
st r1, r0, 0x0
halt
